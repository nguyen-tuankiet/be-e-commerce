package com.example.becommerce.entity.enums;

/**
 * Verification status for technician profiles.
 * Mapped to schema 'verification_status' enum.
 */
public enum VerificationStatus {
    NONE,
    PENDING,
    APPROVED,
    REJECTED
}
